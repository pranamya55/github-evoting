# voter-portal-util-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build voter-portal-util-types` to build the library.

## Running unit tests

Run `nx test voter-portal-util-types` to execute the unit tests via [Jest](https://jestjs.io).
