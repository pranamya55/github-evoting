# landing-page-util-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build landing-page-util-types` to build the library.

## Running unit tests

Run `nx test landing-page-util-types` to execute the unit tests via [Jest](https://jestjs.io).
