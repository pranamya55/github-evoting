#
# (c) Copyright 2025 Swiss Post Ltd.
#

input:
  directory: ${project.build.testOutputDirectory}

direct-trust:
  keystore:
    location: ${project.build.testOutputDirectory}/local_direct_trust_keystore_dispute_resolver.p12
  password:
    location: ${project.build.testOutputDirectory}/local_direct_trust_pw_dispute_resolver.txt
